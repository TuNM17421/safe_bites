packages:
  - "apps/*"
  - "packages/*"

# Prebuilt-binary deps that need their install script approved under pnpm's
# secure-by-default build policy: esbuild (vitest/vite), sharp (Next image optimizer).
allowBuilds:
  esbuild: true
  sharp: true
onlyBuiltDependencies:
  - esbuild
  - sharp
