// Placeholder entry point for @safebite/domain.
//
// The real shared domain lands in Phase 05: types, Zod schemas, the deterministic
// risk engine (allergy + religious/diet/preference mapping, "unknown never suitable"),
// the bilingual question-card builder, safety copy, and constants.
//
// Kept as an intentional placeholder so `tsc --noEmit` has an input and the workspace
// can resolve the `@safebite/domain` alias before Phase 05 fills it in.
export {};
